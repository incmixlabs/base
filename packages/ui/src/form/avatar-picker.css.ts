// ── Static base classes (tailwind) ──

/** Root wrapper for trigger-mode AvatarPicker */
export const avatarPickerRootCls = 'relative w-full'

/** Trigger content area: avatar(s) + text display */
export const avatarPickerTriggerContentCls = 'min-w-0 flex flex-1 items-center gap-2'

/** Trigger action buttons: clear + chevron */
export const avatarPickerTriggerActionsCls = 'flex items-center gap-1'

/** Inline wrapper — minimum width for embedded picker */
export const avatarPickerInlineCls = 'min-w-[12rem]'

/** Single-select dropdown: absolute positioning below trigger */
export const avatarPickerDropdownCls = 'absolute z-50 mt-1 w-full min-w-[8rem] animate-in fade-in-0 zoom-in-95'
